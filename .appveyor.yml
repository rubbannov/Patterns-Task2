image: Ubuntu

stack: jdk 11

branches:
  only:
    - main

build: off

install:

  - java -jar app-ibank.jar -P:profile=test &

build_script:
  - chmod +x gradlew
  ##- ./gradlew test -Dselenide.headless=true --info